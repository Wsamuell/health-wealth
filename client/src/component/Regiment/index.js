import React from 'react';

function Regiment () {
    return (
        <div>
            {/* Here, we would make a query to get all of the user's goals from the database.
            Then all the goals would be formatted as cards, maybe using forEach. Depending on 
            how we want to do it, we could even have a Goal component to help with this. */}
        </div>
    )
}

export default Regiment;