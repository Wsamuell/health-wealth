import React from 'react';

function AutoPost () {
    return (
        <div>
            {/* Here, we would make a query to get all of the user's recipes from the database.
            Then all the recipes would be formatted as cards, maybe using forEach. */}
        </div>
    )
}

export default AutoPost;